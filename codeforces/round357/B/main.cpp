#include <stdio.h>
#include <iostream>
#include <vector>
#include <deque>
#include <cstring>
#include <string>
#include <map>
#include <set>
#include <list>
#include <algorithm>
using namespace std;

using i64 = long long;

bool Solve(i64 N) {
    //
}

int main() {
    i64 N;
    cin >> N;
    cout << (Solve(N) ? "YES" : "NO") << endl;
    return 0;
}
