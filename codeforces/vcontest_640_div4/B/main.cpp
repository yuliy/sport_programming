#include <iostream>
#include <vector>
using namespace std;

void Solve(int n, int k) {
    //
}

int main() {
    cin.tie(0);
    ios_base::sync_with_stdio(0);

    int t;
    cin >> t;

    for (int i = 0; i < t; ++i) {
        int n, k;
        cin >> n >> k;
        Solve(n, k);
    }

    return 0;
}
