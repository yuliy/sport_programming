#include <iostream>
#include <stdlib.h>
#include <cmath>
#include <stdio.h>
#include <cmath>
#include <set>
using namespace std;

int main() {
    int N, M, K;
    cin >> N >> M >> K;

    for (int i = 0; i < K; ++i) {
        int x, y;
        scanf("%d %d", &x, &y);
    }

    return 0;
}
