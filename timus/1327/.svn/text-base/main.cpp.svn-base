#include <stdio.h>
#include <iostream>
#include <vector>
#include <string>
#include <map>
#include <set>
#include <list>
#include <algorithm>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    int res = 0;
    for (int i = a; i <= b; ++i) {
        if (i % 2)
            ++res;
    }

    cout << res << endl;
    return 0;
}
