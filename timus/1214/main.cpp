#include <stdio.h>
#include <iostream>
#include <vector>
#include <string>
#include <map>
#include <set>
#include <list>
#include <algorithm>
using namespace std;

int main() {
    long x, y;
    cin >> x >> y;
    if (x > 0 && y > 0) {

    } else {
        cout << x << ' ' << y << endl;
    }
    return 0;
}
