#include <stdio.h>
#include <iostream>
#include <vector>
#include <string>
#include <map>
#include <set>
#include <list>
#include <algorithm>
using namespace std;

int main() {
    int t1, t2;
    cin >> t1 >> t2;

    int c2 = 0;
    for (int i = 0; i < 10; ++i) {
        int tmp;
        cin >> tmp;
        c2 += tmp;
    }

    t2 -= (c2 * 20);

    cout << ((t1 <= t2) ? "No chance." : "Dirty debug :(") << endl;

    return 0;
}
