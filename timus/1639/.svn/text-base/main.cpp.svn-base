#include <stdio.h>
#include <iostream>
#include <vector>
#include <string>
#include <map>
#include <set>
#include <list>
#include <algorithm>
using namespace std;

int main() {
    int m, n;
    cin >> m >> n;

    const int r = (m - 1) + m * (n - 1);
    if (r % 2)
        printf("[:=[first]\n");
    else
        printf("[second]=:]\n");

    return 0;
}
