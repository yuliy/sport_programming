#include <stdio.h>
#include <iostream>
#include <vector>
#include <string>
#include <map>
#include <set>
#include <list>
#include <algorithm>
using namespace std;

const int MAX_POW = 1000;
const int MAX_X = 1000;
int POW[MAX_X][MAX_POW];

int N, M, Y;

static void Init() {
    //
}

int main() {
    scanf("%d %d %d", &N, &M, &Y);
    Init();

    return 0;
}
